import React from "react";

export const metadata = {
  title: "401(k) Calculator",
  description:
    "Free 401(k) Calculator to estimate retirement savings growth, employer matching contributions, investment returns, and future retirement income projections.",
};

export default function Retirement401kLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}