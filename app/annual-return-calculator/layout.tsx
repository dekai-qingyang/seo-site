import React from "react";

export const metadata = {
  title: "Annual Return Calculator",
  description:
    "Free annual return calculator to estimate yearly investment performance, annualized returns, portfolio growth, and long-term investment gains.",
};

export default function AnnualReturnCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}