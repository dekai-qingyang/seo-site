import React from "react";

export const metadata = {
  title: "APR Calculator",
  description:
    "Free APR calculator to estimate annual percentage rate, monthly payments, total interest costs, and borrowing expenses for loans and credit products.",
};

export default function APRCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}