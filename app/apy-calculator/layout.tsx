import React from "react";

export const metadata = {
  title: "APY Calculator",
  description:
    "Free APY calculator to estimate annual percentage yield, compound interest earnings, savings growth, investment returns, and future account balances.",
};

export default function APYCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}