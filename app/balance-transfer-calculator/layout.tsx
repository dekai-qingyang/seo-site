import React from "react";

export const metadata = {
  title: "Balance Transfer Calculator",
  description:
    "Free balance transfer calculator to estimate credit card payoff savings, transfer fees, monthly payments, and interest reduction strategies.",
};

export default function BalanceTransferLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}