import React from "react";

export const metadata = {
  title: "Bank Interest Calculator",
  description:
    "Free Bank Interest Calculator to estimate savings growth, compound interest earnings, APY returns, and future account balances.",
};

export default function BankInterestCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}