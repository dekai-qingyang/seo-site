import React from "react";

export const metadata = {
  title: "Bonus Tax Calculator",
  description:
    "Free Bonus Tax Calculator to estimate bonus taxes, withholding rates, net bonus income, paycheck deductions, and take-home bonus pay projections.",
};

export default function BonusTaxLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}