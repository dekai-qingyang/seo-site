import React from "react";

export const metadata = {
  title: "Break Even Calculator",
  description:
    "Free Break Even Calculator to estimate break-even points, fixed costs, profit targets, business margins, and revenue projections.",
};

export default function BreakEvenLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}