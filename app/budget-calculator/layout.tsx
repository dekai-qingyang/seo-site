import React from "react";

export const metadata = {
  title: "Budget Calculator",
  description:
    "Free Budget Calculator to estimate monthly expenses, savings, cash flow, spending categories, and long-term financial planning.",
};

export default function BudgetCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}