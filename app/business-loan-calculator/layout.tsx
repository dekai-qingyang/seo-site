import React from "react";

export const metadata = {
  title: "Business Loan Calculator",
  description:
    "Free Business Loan Calculator to estimate monthly payments, business loan interest, amortization schedules, total repayment costs, and financing projections.",
};

export default function BusinessLoanLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}