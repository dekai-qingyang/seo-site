import React from "react";

export const metadata = {
  title: "Capital Gains Calculator",
  description:
    "Free capital gains calculator to estimate investment profits, capital gains taxes, annual returns, and after-tax investment performance.",
};

export default function CapitalGainsCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}