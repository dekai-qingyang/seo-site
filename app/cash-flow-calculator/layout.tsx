import React from "react";

export const metadata = {
  title: "Cash Flow Calculator",
  description:
    "Free Cash Flow Calculator to analyze income, expenses, savings, and investments, with detailed monthly projections, visual charts, and actionable financial insights.",
};

export default function CashFlowLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}