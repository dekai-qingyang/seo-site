import React from "react";

export const metadata = {
  title: "Cash Runway Calculator",
  description:
    "Free Cash Runway Calculator to estimate startup runway, monthly burn rate, operating expenses, funding duration, and business survival projections.",
};

export default function CashRunwayLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}