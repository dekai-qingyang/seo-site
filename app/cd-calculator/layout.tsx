import React from "react";

export const metadata = {
  title: "CD Calculator",
  description:
    "Free CD Calculator to estimate certificate of deposit growth, APY returns, compound interest earnings, and long-term savings projections.",
};

export default function CDCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}