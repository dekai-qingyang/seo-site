import React from "react";

export const metadata = {
  title: "Certificate of Deposit Calculator",
  description:
    "Free certificate of deposit calculator to estimate CD interest earnings, future value, compound growth, APY returns, and long-term savings projections.",
};

export default function CertificateOfDepositCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}