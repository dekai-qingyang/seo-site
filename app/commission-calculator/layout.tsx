import React from "react";

export const metadata = {
  title: "Commission Calculator",
  description:
    "Free Commission Calculator to estimate sales commissions, commission rates, bonuses, taxes, and total take-home earnings projections.",
};

export default function CommissionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}