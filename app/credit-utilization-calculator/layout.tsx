import React from "react";

export const metadata = {
  title: "Credit Utilization Calculator",
  description:
    "Free credit utilization calculator to estimate credit usage ratios, improve credit scores, and manage revolving debt balances.",
};

export default function CreditUtilizationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}