export const metadata = {
    title: "Debt Consolidation Calculator",
    description:
      "Free debt consolidation calculator to estimate monthly payments, consolidation savings, interest reduction, and debt payoff timelines.",
  };
  
  export default function DebtConsolidationLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return children;
  }