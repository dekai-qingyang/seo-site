import React from "react";

export const metadata = {
  title: "Discount Calculator",
  description:
    "Free Discount Calculator to estimate sale prices, savings amounts, markdown percentages, retail discounts, and pricing projections.",
};

export default function DiscountLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}