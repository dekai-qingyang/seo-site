import React from "react";

export const metadata = {
  title: "Emergency Fund Calculator",
  description:
    "Free Emergency Fund Calculator to estimate ideal emergency savings, monthly expense coverage, financial safety targets, and cash reserve planning.",
};

export default function EmergencyFundCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}