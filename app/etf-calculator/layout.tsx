import React from "react";

export const metadata = {
  title: "ETF Calculator",
  description:
    "Free ETF calculator to estimate ETF investment growth, compound returns, portfolio performance, dividend reinvestment, and long-term wealth accumulation.",
};

export default function ETFCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}