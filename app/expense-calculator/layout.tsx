import React from "react";

export const metadata = {
  title: "Expense Calculator",
  description:
    "Free Expense Calculator to estimate monthly expenses, track spending categories, analyze cash flow, improve budgeting, and optimize financial planning with interactive charts and detailed expense analysis.",
};

export default function ExpenseCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}