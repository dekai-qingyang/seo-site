import React from "react";

export const metadata = {
  title: "Financial Independence Calculator",
  description:
    "Free financial independence calculator to estimate FIRE goals, retirement savings, passive income, and long-term financial freedom timelines.",
};

export default function FinancialIndependenceCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}