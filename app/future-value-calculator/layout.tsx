import React from "react";

export const metadata = {
  title: "Future Value Calculator",
  description:
    "Free future value calculator to estimate compound investment growth, future savings value, investment returns, and long-term financial planning.",
};

export default function FutureValueCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}