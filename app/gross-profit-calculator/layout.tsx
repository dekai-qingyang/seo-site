import React from "react";

export const metadata = {
  title: "Gross Profit Calculator",
  description:
    "Free Gross Profit Calculator to estimate gross profit, gross margin, business costs, revenue growth, and long-term profitability projections.",
};

export default function GrossProfitLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}