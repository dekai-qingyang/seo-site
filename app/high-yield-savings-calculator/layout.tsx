import React from "react";

export const metadata = {
  title: "High Yield Savings Calculator",
  description:
    "Free High Yield Savings Calculator to estimate APY growth, compound interest earnings, future savings balances, and long-term wealth accumulation.",
};

export default function HighYieldSavingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}