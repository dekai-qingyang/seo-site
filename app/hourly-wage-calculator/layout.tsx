import React from "react";

export const metadata = {
  title: "Hourly Wage Calculator",
  description:
    "Free Hourly Wage Calculator to estimate hourly pay, annual salary, overtime income, taxes, deductions, and take-home paycheck projections.",
};

export default function HourlyWageLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}