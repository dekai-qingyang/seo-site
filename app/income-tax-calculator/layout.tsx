import React from "react";

export const metadata = {
  title: "Income Tax Calculator",
  description:
    "Free Income Tax Calculator to estimate federal income taxes, state taxes, deductions, taxable income, refunds, and take-home pay projections.",
};

export default function IncomeTaxLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}