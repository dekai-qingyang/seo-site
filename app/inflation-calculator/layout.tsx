import React from "react";

export const metadata = {
  title: "Inflation Calculator",
  description:
    "Free inflation calculator to estimate purchasing power, inflation impact, future prices, and long-term money value using compound inflation projections.",
};

export default function InflationCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}