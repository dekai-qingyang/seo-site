export const metadata = {
    title: "Interest Payoff Calculator",
    description:
      "Free interest payoff calculator to estimate loan repayment timelines, total interest costs, payoff strategies, monthly payment schedules, and debt reduction savings.",
  };
  
  export default function InterestPayoffLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return children;
  }