import React from "react";

export const metadata = {
  title: "Investment Return Calculator",
  description:
    "Free investment return calculator to estimate portfolio growth, annual returns, compound interest, and long-term investment performance.",
};

export default function InvestmentReturnCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}