import React from "react";

export const metadata = {
  title: "IRA Calculator",
  description:
    "Free IRA Calculator to estimate retirement savings growth, annual contributions, compound interest, and future retirement income projections.",
};

export default function IRALayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}