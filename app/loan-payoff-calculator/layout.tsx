export const metadata = {
    title: "Loan Payoff Calculator",
    description:
      "Free loan payoff calculator to estimate monthly payments, payoff timelines, total interest costs, debt repayment schedules, and early payoff savings.",
  };
  
  export default function LoanPayoffLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return children;
  }