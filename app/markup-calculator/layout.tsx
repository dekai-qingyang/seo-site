import React from "react";

export const metadata = {
  title: "Markup Calculator",
  description:
    "Free Markup Calculator to estimate selling prices, markup percentages, profit margins, product pricing, and business profitability projections.",
};

export default function MarkupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}