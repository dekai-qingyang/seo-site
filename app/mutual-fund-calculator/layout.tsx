import React from "react";

export const metadata = {
  title: "Mutual Fund Calculator",
  description:
    "Free mutual fund calculator to estimate investment growth, compound returns, portfolio performance, dividend reinvestment, and long-term wealth accumulation.",
};

export default function MutualFundCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}