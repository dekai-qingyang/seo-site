import React from "react";

export const metadata = {
  title: "Net Profit Calculator",
  description:
    "Free Net Profit Calculator to estimate net income, operating expenses, taxes, profit margins, and long-term business profitability projections.",
};

export default function NetProfitLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}