import React from "react";

export const metadata = {
  title: "Net Worth Calculator",
  description:
    "Free net worth calculator to estimate total assets, liabilities, personal wealth, financial progress, and long-term financial health.",
};

export default function NetWorthCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}