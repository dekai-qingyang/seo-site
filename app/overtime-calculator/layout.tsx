import React from "react";

export const metadata = {
  title: "Overtime Calculator",
  description:
    "Free Overtime Calculator to estimate overtime pay, extra work hours, overtime wages, taxes, and total take-home income projections.",
};

export default function OvertimeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}