import React from "react";

export const metadata = {
  title: "Paycheck Calculator",
  description:
    "Free Paycheck Calculator to estimate gross pay, paycheck taxes, deductions, overtime income, and net take-home salary projections.",
};

export default function PaycheckLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}