import React from "react";

export const metadata = {
  title: "Pension Calculator",
  description:
    "Free Pension Calculator to estimate retirement pension income, future retirement savings, monthly payouts, and long-term retirement planning projections.",
};

export default function PensionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}