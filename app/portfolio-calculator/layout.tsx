import React from "react";

export const metadata = {
  title: "Portfolio Calculator",
  description:
    "Free portfolio calculator to estimate investment allocation, compound growth, diversification, annual returns, and long-term portfolio performance.",
};

export default function PortfolioCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}