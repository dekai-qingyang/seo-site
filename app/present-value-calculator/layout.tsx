import React from "react";

export const metadata = {
  title: "Present Value Calculator",
  description:
    "Free present value calculator to estimate discounted cash flow values, investment valuation, future cash worth, and financial planning analysis.",
};

export default function PresentValueCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}