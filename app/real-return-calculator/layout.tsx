import React from "react";

export const metadata = {
  title: "Real Return Calculator",
  description:
    "Free real return calculator to estimate inflation-adjusted investment returns, real portfolio growth, purchasing power, and long-term investment performance.",
};

export default function RealReturnCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}