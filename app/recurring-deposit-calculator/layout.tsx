import React from "react";

export const metadata = {
  title: "Recurring Deposit Calculator",
  description:
    "Free Recurring Deposit Calculator to estimate RD maturity amount, compound interest earnings, monthly savings growth, and long-term fixed deposit returns.",
};

export default function RecurringDepositLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}