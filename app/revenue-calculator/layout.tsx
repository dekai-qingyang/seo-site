import React from "react";

export const metadata = {
  title: "Revenue Calculator",
  description:
    "Free Revenue Calculator to estimate sales revenue, business growth, operating income, customer value, and long-term financial projections.",
};

export default function RevenueLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}