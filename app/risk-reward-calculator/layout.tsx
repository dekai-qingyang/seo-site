import React from "react";

export const metadata = {
  title: "Risk Reward Calculator",
  description:
    "Free risk reward calculator to estimate trading risk, reward potential, stop loss analysis, position sizing, portfolio risk management, and investment performance.",
};

export default function RiskRewardCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}