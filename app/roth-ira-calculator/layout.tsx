import React from "react";

export const metadata = {
  title: "Roth IRA Calculator",
  description:
    "Free Roth IRA Calculator to estimate retirement savings growth, tax-free withdrawals, compound investment returns, and long-term retirement projections.",
};

export default function RothIRALayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}