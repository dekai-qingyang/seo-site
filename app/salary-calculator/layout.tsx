import React from "react";

export const metadata = {
  title: "Salary Calculator",
  description:
    "Free Salary Calculator to estimate annual salary, monthly income, hourly wages, taxes, deductions, and net paycheck projections.",
};

export default function SalaryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}