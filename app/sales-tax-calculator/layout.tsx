import React from "react";

export const metadata = {
  title: "Sales Tax Calculator",
  description:
    "Free Sales Tax Calculator to estimate sales taxes, total purchase costs, tax-inclusive pricing, savings, and long-term spending projections.",
};

export default function SalesTaxLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}