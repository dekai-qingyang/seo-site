import React from "react";

export const metadata = {
  title: "Simple Interest Calculator",
  description:
    "Free simple interest calculator to estimate loan interest, investment earnings, savings growth, and total repayment costs.",
};

export default function SimpleInterestLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}