import React from "react";

export const metadata = {
  title: "Social Security Calculator",
  description:
    "Free Social Security Calculator to estimate retirement benefits, monthly Social Security income, retirement age projections, and future retirement planning.",
};

export default function SocialSecurityLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}