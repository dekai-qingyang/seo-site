import React from "react";

export const metadata = {
  title: "Stock Calculator",
  description:
    "Free stock calculator to estimate investment returns, stock profits, dividends, annual growth, portfolio performance, and long-term wealth accumulation.",
};

export default function StockCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}