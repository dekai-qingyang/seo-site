export const metadata = {
    title: "Student Loan Calculator",
    description:
      "Free student loan calculator with monthly payment estimates, payoff charts, interest analysis, repayment comparisons, and student debt planning tools.",
  };
  
  export default function StudentLoanLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return children;
  }