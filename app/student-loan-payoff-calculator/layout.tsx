export const metadata = {
    title: "Student Loan Payoff Calculator",
    description:
      "Free student loan payoff calculator with repayment estimates, payoff schedules, interest analysis, debt reduction planning, and student loan payoff charts.",
  };
  
  export default function StudentLoanPayoffLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return children;
  }