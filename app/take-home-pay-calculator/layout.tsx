import React from "react";

export const metadata = {
  title: "Take Home Pay Calculator",
  description:
    "Free Take Home Pay Calculator to estimate net income, paycheck deductions, taxes, take-home salary, and monthly income projections.",
};

export default function TakeHomePayLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}