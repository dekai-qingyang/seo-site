import React from "react";

export const metadata = {
  title: "Tax Calculator",
  description:
    "Free Tax Calculator to estimate income taxes, federal taxes, state taxes, deductions, refunds, and net take-home income projections.",
};

export default function TaxLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}