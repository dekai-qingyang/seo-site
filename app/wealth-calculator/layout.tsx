import React from "react";

export const metadata = {
  title: "Wealth Calculator",
  description:
    "Free wealth calculator to estimate long-term wealth growth, investments, savings, passive income, and financial independence progress.",
};

export default function WealthCalculatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}